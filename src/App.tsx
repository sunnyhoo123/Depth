import vue from "vue"
import vueClassComponent from "vue-class-component"

// @vueClassComponent({
// })
export default class App extends vue {
  created() {

  }
  render(h) {
    return (
      <div>
        123
      </div>
    );
  }
}